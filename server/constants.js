const {SERVER_TOKEN} = require('../config/auth');

module.exports = {
    APP_API_PREFIX: '/api/v1',
    BASE_UBER_API_URL: 'https://api.uber.com/v1.2',
    SERVER_TOKEN,

    DISPLAY_NAME_PREFIX: 'After'
};
